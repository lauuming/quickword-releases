# Quickword releases

Installers and the auto-update feed for Quickword. Binaries only; the source lives in a private repository.

Windows: download the latest `Quickword_x.y.z_x64-setup.exe` from Releases. macOS builds follow.

Built on Handy by CJ Pais (MIT).
